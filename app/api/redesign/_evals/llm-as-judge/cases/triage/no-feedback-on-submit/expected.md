## AOI 1

Submitting the add-item form gives no visible confirmation that it worked — the text input isn't cleared and nothing on screen changes to indicate the task was added, even though it was. Users can't tell whether their click registered, leaving them uncertain if the task actually got added or if they need to try again.